<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
             xmlns:p="tapestry:parameter" >
<t:content>
    <div>

        <div>
            <table class="table table-hover text-lg">
                <tr>
                    <th>#</th>
                    <th>${message:given-answer}</th>
                    <th>${message:correct-answer}</th>
                </tr>

                <t:loop source="givenAnswersMap.keySet()" value="numEl">
                    <tr>
                        <td>${viewableNumber}</td>
                            <td>
                                <t:loop source="givenAnswersMap.get(numEl)" value="el" >
                                    <t:if test="el.correct">
                                        <p class="bg-success">${el.answerVariant.readableText}</p>
                                        <p:else>
                                            <p class="bg-danger">${el.answerVariant.readableText}</p>
                                        </p:else>
                                    </t:if>
                                </t:loop>
                            </td>
                            <td>
                                <t:if test="correctAnswerExist" >
                                    <p>${orderedCorrectAnswers.get(numEl).readableText}</p>
                                </t:if>
                            </td>
                     </tr>
                </t:loop>
            </table>
        </div>

    </div>
</t:content>
</html>