<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
             xmlns:p="tapestry:parameter" >
<t:content>
    <div>
        <t:loop source="questionList" value="correspondenceVariant">
            <div class="row">
                <h4>${correspondenceVariant.text}</h4>
                <div class="col-sm-10 col-lg-offset-2">
                    <h5>${message:selected-answers}</h5>
                    <t:loop source="chooseAnswersList(correspondenceVariant)" value="answerVariant">
                        <t:if test="checkCorrectAnswer(answerVariant)">
                            <p class="bg-success">${answerVariant.readableText}</p>
                            <p:else>
                                <p class="bg-danger">${answerVariant.readableText}</p>
                            </p:else>
                        </t:if>
                    </t:loop>

                    <t:if test="!missedCorrectAnswers.empty">
                        <div>
                            <h5>${message:missed-correct-answers}</h5>
                            <t:loop source="missedCorrectAnswers" value="answerVariant">
                                <p>${answerVariant.readableText}</p>
                            </t:loop>
                        </div>
                    </t:if>
                </div>
            </div>
        </t:loop>
    </div>
</t:content>
</html>