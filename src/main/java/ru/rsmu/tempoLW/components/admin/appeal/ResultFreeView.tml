<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
      xmlns:p="tapestry:parameter" >
<t:content>
    <div>
        <h5>${message:given-answer}</h5>
        <t:if test="current.answered">
            <t:loop source="resultFree.files" value="file">
                <p><t:pagelink page="admin/resultattachment" context="file.id">${file.sourceName}</t:pagelink> </p>
            </t:loop>
            <p:else>
                <p>${message:no-answer}</p>
            </p:else>
        </t:if>
    </div>

    <t:if test="current.answered">
        <h5>${message:given-score}</h5>
        <div class="content">
            <t:loop source="resultFree.evaluation" value="eval" empty="block:noScores">
                <div class="col-sm-4">${eval.explanation}</div>
                <div class="col-sm-2">${eval.score}</div>
                <div class="col-sm-4">${eval.examiner.lastName}</div>
                <div class="col-sm-2"><t:output format="literal:dd.MM.yyyy" value="eval.updated"/></div>
            </t:loop>
            <t:block id="noScores">
                ${message:noScoreYet}
            </t:block>
        </div>

        <h5>${message:set-score}</h5>
        <t:form t:id="resultEvaluationForm" validate="evaluation" class="form-horizontal" >
            <div class="form-group">
                <t:label for="evaluationScore" class="col-sm-4"/>
                <div class="col-sm-6">
                    <t:textfield t:id="evaluationScore" value="evaluationScore"/>
                </div>
            </div>
            <div class="form-group">
                <t:label for="scoreExplanation" class="col-sm-4"/>
                <div class="col-sm-6">
                    <t:textarea t:id="scoreExplanation" value="evaluation.explanation"/>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-4 col-sm-offset-4">
                    <t:submit value="message:save" class="btn btn-primary" mixins="ClickOnce"/>
                </div>
            </div>
        </t:form>
    </t:if>

</t:content>
</html>